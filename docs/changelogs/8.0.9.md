# BLU 8.0.9

## Fixed

- Restored the WoW Forever Beta interface `120007` to the TOC. MR !5 had dropped it and mislabeled it as a stale Retail entry; the Forever Beta client build `1.60.1.69893` validated in #6 uses interface `120007`, and removing it flagged the addon as out-of-date on that client.
- TOC interface list is now `120007,120100,16001`: WoW Forever Beta `120007`, Retail live `120100`, and the additional beta flavor `16001` are all declared so the addon loads cleanly on every supported client.

## Notes

- No code behavior changed; this is metadata-only compatibility restoration.
- Release scope per #6: semantic patch bump, capability-based detection retained, stable-client compatibility and distribution IDs preserved.
