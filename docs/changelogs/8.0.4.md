## v8.0.4 - 2026-08-13

### Fixed

- Fixed repeated `UNIT_AURA` callback errors when Midnight marks `updateInfo.addedAuras` as secret.
- Removed BLU's direct inspection and iteration of the raw aura event payload.
- Routed generic proc notifications through `RGXCombat` and Lust/Heroism detection through `RGXAuras:HasPlayerAura`, with edge detection so the dedicated sound fires once per gain.

Requires RGX-Framework v2.5.1 or later.
